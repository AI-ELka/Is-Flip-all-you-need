# This example trains a single expert and generates poisoned labels
# for the sinusoidal (4xl) trigger with ResNet-32s. The labels are 
# FLIPped at the provided budgets. The config file is broken down 
# into three modules detailed in the schemas/ folder.

# Module to train and record an expert trajectory.
[train_expert]
output_dir = "out/checkpoints/r32p_4xl/0/"
model = "r32p"
trainer = "sgd"
dataset = "svhn"
source_label = 9
target_label = 4
poisoner = "4xl"
epochs = 20
checkpoint_iters = 50

# Module to generate attack labels from the expert trajectories.
[generate_labels]
input_pths = "out/checkpoints/r32p_4xl/{}/model_{}_{}.pth"
opt_pths = "out/checkpoints/r32p_4xl/{}/model_{}_{}_opt.pth"
expert_model = "r32p"
trainer = "sgd"
dataset = "svhn"
source_label = 9
target_label = 4
poisoner = "4xl"
output_dir = "experiments/svhn_4xl/"
lambda = 0.0

[generate_labels.expert_config]
experts = 1
min = 0
max = 20
trajectories = [50, 100, 150, 200, 250, 300]

[generate_labels.attack_config]
iterations = 15
one_hot_temp = 5
alpha = 0
label_kwargs = {lr = 150, momentum = 0.5}

# Module to flip labels at the provided budgets.
[select_flips]
budgets = [150, 300, 500, 1000, 1500, 2000, 2500]
input_label_glob = "experiments/svhn_4xl/labels.npy"
true_labels = "experiments/svhn_4xl/true.npy"
output_dir = "experiments/svhn_4xl/"
